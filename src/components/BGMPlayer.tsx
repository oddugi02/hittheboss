'use client';

export default function BGMPlayer() {
  // Disabled: the generated ambient loop sounded like repeating SFX and kept
  // playing after the first interaction even when the user was idle.
  return null;
}
